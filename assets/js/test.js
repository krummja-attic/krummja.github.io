d3.select("div#js-tests")
    .append("svg")
        .attr('width', 600)
        .attr('height', 200)
    .append("rect")
        .attr('width', 500)
        .attr('height', 150)
        .style('color', 'red')
